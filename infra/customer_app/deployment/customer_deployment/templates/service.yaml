apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer_deployment.fullname" . }}
  labels:
    {{- include "customer_deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customer_deployment.selectorLabels" . | nindent 4 }}
